# from setuptools import setup, find_packages

# setup(
#     name="FRONT_SEPP_3",        # Replace with your project's name
#     version="0.1.0",
#     packages=find_packages(where="src"),  # Look for packages in the 'src' directory
#     package_dir={"": "src"},         # Define the root directory for packages
#     install_requires=[],             # Add dependencies if any
#     python_requires=">=3.7",         # Specify your Python version requirement
# )

from setuptools import setup

if __name__ == "__main__":
    setup()